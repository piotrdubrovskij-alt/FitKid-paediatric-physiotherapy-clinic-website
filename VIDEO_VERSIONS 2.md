# 🎬 Версии видео (с титрами и без)

## ✅ Активная версия (используется на сайте)

**Файл:** `public/media/hero.mp4` (7.5 MB)
- ✂️ Длительность: 20 секунд
- 📐 Разрешение: 1920x1080 (Full HD)
- 🎨 Эффект: **Темный градиент снизу** (скрывает титры)
- ⚡ Загрузка: ~1 секунда

**Почему градиент:**
- Современный, стильный вид для hero-секции
- Титры полностью скрыты
- Полный кадр видео сохранен
- Меньший размер файла

---

## 📦 Альтернативные версии

### 1. С обрезкой снизу (без градиента)
**Файл:** `public/media/hero-cropped.mp4` (8.2 MB)
- Нижняя часть видео обрезана (где были титры)
- Титры удалены полностью
- Без градиента

**Как использовать:**
```bash
cd public/media
mv hero.mp4 hero-gradient.mp4
cp hero-cropped.mp4 hero.mp4
```

### 2. Оригинал с титрами
**Файл:** `public/media/hero-with-subtitles-backup.mp4` (8.6 MB)
- Резервная копия с титрами
- На случай если нужно вернуть

**Как использовать:**
```bash
cd public/media
cp hero-with-subtitles-backup.mp4 hero.mp4
```

---

## 🗑️ Файлы для удаления (опционально)

### Оригинал (очень большой)
**Файл:** `public/media/hero.mov` (296 MB ❗)
- Исходный файл в 4K
- Больше не нужен (все версии созданы)

**Удалить:**
```bash
rm public/media/hero.mov
# Освободит 296 MB места
```

### Резервные копии
Если уверены что current версия подходит:
```bash
rm public/media/hero-with-subtitles-backup.mp4  # Освободит 8.6 MB
rm public/media/hero-cropped.mp4                # Освободит 8.2 MB
```

---

## 🎨 Как изменить градиент (если нужно)

### Сделать градиент светлее/темнее

**Текущий код (в ffmpeg):**
```bash
drawbox=x=0:y=ih-200:w=iw:h=200:color=black@0.7:t=fill
```

**Параметры:**
- `y=ih-200` — начало градиента (200px снизу)
- `h=200` — высота градиента (200px)
- `color=black@0.7` — цвет и прозрачность (0.7 = 70% непрозрачность)

**Изменить прозрачность:**
```bash
# Темнее (90% непрозрачный)
color=black@0.9

# Светлее (50% непрозрачный)
color=black@0.5

# Еще светлее (30%)
color=black@0.3
```

**Изменить высоту:**
```bash
# Выше (300px)
y=ih-300:h=300

# Ниже (150px)
y=ih-150:h=150
```

**Пересоздать видео с новыми параметрами:**
```bash
/tmp/ffmpeg -i public/media/hero.mov \
  -t 20 \
  -vf "scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2,fps=30,drawbox=x=0:y=ih-150:w=iw:h=150:color=black@0.5:t=fill" \
  -c:v libx264 \
  -preset medium \
  -crf 23 \
  -movflags +faststart \
  -an \
  public/media/hero-custom.mp4 \
  -y
```

---

## 📊 Сравнение версий

| Версия | Размер | Титры | Эффект | Рекомендация |
|--------|--------|-------|--------|--------------|
| **hero.mp4** (текущая) | 7.5 MB | Скрыты | Темный градиент | ⭐ Лучший вариант |
| hero-cropped.mp4 | 8.2 MB | Удалены | Обрезка снизу | Если не нравится градиент |
| hero-with-subtitles-backup.mp4 | 8.6 MB | Видны | Нет | Резервная копия |
| hero.mov (оригинал) | 296 MB | Видны | Нет | Удалить ❌ |

---

## ✨ Финальный результат

**Было:**
- ❌ 296 MB (огромный файл)
- ❌ 44 секунды (слишком длинное)
- ❌ 4K разрешение (избыточно)
- ❌ Титры видны

**Стало:**
- ✅ 7.5 MB (в 39.5 раз меньше!)
- ✅ 20 секунд (идеально для hero)
- ✅ Full HD (оптимально)
- ✅ Титры скрыты (стильный градиент)
- ✅ Загрузка < 2 секунд ⚡

---

## 🌐 Проверка

Откройте: `http://localhost:3000`

Видео должно:
- ✅ Автоматически воспроизводиться
- ✅ Не показывать титры (скрыты градиентом)
- ✅ Загружаться быстро
- ✅ Выглядеть профессионально

---

Готово! 🎉
