# 🎬 Как обрезать и оптимизировать видео для сайта

Ваше видео `hero.mov` — **296 МБ**, это слишком много для веб-сайта!

Нужно:
- ✂️ Обрезать до 15-20 секунд
- 🗜️ Сжать до 3-5 МБ
- 📦 Конвертировать в MP4 (веб-формат)

---

## ⚡ Быстрый способ (без установки программ)

### Онлайн-сервис — 2 минуты

**1. Откройте:** https://www.freeconvert.com/video-compressor

**2. Загрузите** файл:
```
/Users/piotrdubrovskij/Desktop/cursor/fitkid-website/public/media/hero.mov
```

**3. Настройки:**
- **Trim**: Обрежьте до 20 секунд (0:00 → 0:20)
- **Output format**: MP4
- **Resolution**: 1920x1080 (Full HD)
- **Quality**: Medium

**4. Нажмите:** "Convert"

**5. Скачайте** результат и сохраните как:
```
/Users/piotrdubrovskij/Desktop/cursor/fitkid-website/public/media/hero.mp4
```

**6. Обновите** страницу `http://localhost:3000` — готово!

---

## 💻 Профессиональный способ (с ffmpeg)

### Шаг 1: Установите ffmpeg

**Терминал:**
```bash
# 1. Установите Homebrew (если ещё нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. Установите ffmpeg
brew install ffmpeg
```

Это займёт 3-5 минут.

### Шаг 2: Запустите автоматический скрипт

Я уже создал готовый скрипт `convert-video.sh` в корне проекта.

**Терминал:**
```bash
cd /Users/piotrdubrovskij/Desktop/cursor/fitkid-website
./convert-video.sh
```

**Что сделает скрипт:**
1. ✂️ Обрежет видео до 20 секунд
2. 🗜️ Сожмёт до ~3-5 МБ
3. 📦 Создаст MP4 (для всех браузеров)
4. 📦 Создаст WebM (лучшее сжатие)
5. 📸 Создаст poster image (картинка до загрузки)

**Результат:**
```
public/media/
├── hero.mp4        ← Готово! (~3-5MB)
├── hero.webm       ← Альтернатива (~2-3MB)
└── hero-poster.jpg ← Постер
```

### Шаг 3: Удалите старый файл (опционально)

```bash
rm public/media/hero.mov
```

Освободит 296 МБ места!

---

## 🎯 Ручная команда (если нужен полный контроль)

Если хотите сами настроить параметры:

```bash
cd /Users/piotrdubrovskij/Desktop/cursor/fitkid-website

# Обрезать до 20 секунд + сжать
ffmpeg -i public/media/hero.mov \
  -t 20 \
  -vf "scale=1920:1080:force_original_aspect_ratio=decrease,fps=30" \
  -c:v libx264 \
  -preset medium \
  -crf 23 \
  -movflags +faststart \
  -an \
  public/media/hero.mp4

# Создать постер (первый кадр)
ffmpeg -i public/media/hero.mp4 \
  -ss 00:00:01 \
  -vframes 1 \
  public/media/hero-poster.jpg
```

**Параметры:**
- `-t 20` — обрезать до 20 секунд
- `-crf 23` — качество (18=высокое, 28=низкое)
- `-preset medium` — скорость конвертации
- `-an` — удалить звук (не нужен для hero video)

---

## 🔧 Настройка длительности

Хотите другую длительность?

**10 секунд:**
```bash
ffmpeg -i public/media/hero.mov -t 10 -c:v libx264 -crf 23 -an public/media/hero.mp4
```

**15 секунд:**
```bash
ffmpeg -i public/media/hero.mov -t 15 -c:v libx264 -crf 23 -an public/media/hero.mp4
```

**Взять середину** (с 5-й по 25-ю секунду):
```bash
ffmpeg -i public/media/hero.mov -ss 00:00:05 -t 20 -c:v libx264 -crf 23 -an public/media/hero.mp4
```

---

## 📊 Целевые размеры

| Длительность | Разрешение | Примерный размер |
|--------------|-----------|------------------|
| 10 секунд | 1920x1080 | 2-3 MB |
| 15 секунд | 1920x1080 | 3-4 MB |
| 20 секунд | 1920x1080 | 4-5 MB |
| 30 секунд | 1920x1080 | 6-8 MB |

**Рекомендация:** 15-20 секунд для hero video.

---

## ✅ Проверка

После конвертации проверьте:

```bash
# Размер файла
ls -lh public/media/hero.mp4

# Длительность
ffmpeg -i public/media/hero.mp4 2>&1 | grep Duration
```

**Хороший результат:**
- Размер: 3-5 MB
- Длительность: 15-20 секунд
- Формат: MP4 (H.264)

---

## 🌐 Проверка на сайте

1. Обновите страницу: `http://localhost:3000`
2. Откройте DevTools (F12) → Network
3. Найдите `hero.mp4` → проверьте размер

**Если видео не появилось:**
- Очистите кэш браузера (Cmd+Shift+R)
- Перезапустите dev сервер: `npm run dev`

---

## 🆘 Проблемы?

### "ffmpeg: command not found"
→ Установите ffmpeg: `brew install ffmpeg`

### "Conversion failed"
→ Проверьте, что файл `hero.mov` существует

### "Video is too large"
→ Увеличьте `-crf` до 25-28 (меньше качество = меньше размер)

### "Video is blurry"
→ Уменьшите `-crf` до 20-22 (выше качество)

---

## 📞 Нужна помощь?

Напишите мне — конвертирую за вас! 🚀
